<%= form_for(@text_reflection) do |f| %>
  <% if @text_reflection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_reflection.errors.count, "error") %> prohibited this text_reflection from being saved:</h2>

      <ul>
      <% @text_reflection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
