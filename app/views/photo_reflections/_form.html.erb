<%= form_for(@photo_reflection, :html => { :multipart => true }) do |f| %>
  <% if @photo_reflection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_reflection.errors.count, "error") %> prohibited this photo_reflection from being saved:</h2>

      <ul>
      <% @photo_reflection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.file_field("uploaded_picture") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
